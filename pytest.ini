[pytest]
testpaths = tests

# Vendored historical/security-replay artifacts carry their own toolchains and
# are evidence, not part of this repository's owned Python test suite.
norecursedirs = artifacts/runtime
